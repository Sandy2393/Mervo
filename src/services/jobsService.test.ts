// Tests moved to the top-level 'tests/' folder so TypeScript compilation of src/ ignores test code.
// This placeholder avoids the project build failing when running tsc in environments where test libs are not installed.

