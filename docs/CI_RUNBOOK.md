# CI Runbook

- Re-run failed jobs from GitHub Actions UI.
- Check artifacts in `test-results` and `playwright-report`.
- For flaky E2E, re-run with trace enabled.
- Promote preview to prod only after manual approval.
