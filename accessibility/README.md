# Accessibility Testing

- Playwright + axe-core integrated in `tests/a11y.playwright.ts`.
- Run with `npx playwright test tests/a11y.playwright.ts`.
- Fix issues by adding labels, aria attributes, and proper contrast.
