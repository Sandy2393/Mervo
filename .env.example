# Existing app env placeholders
VITE_SUPABASE_URL=https://lnaghkkodbguhwhffiyl.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxuYWdoa2tvZGJndWh3aGZmaXlsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ4NTEzMTcsImV4cCI6MjA4MDQyNzMxN30.pTJtS5RDnXbR_rfRQ4HMohWVUwXqGK_qzKOsb85mTaE

# Server-only (DO NOT commit real keys here) - store in CI/Platform secrets
# Supabase service role key (server-only): used for admin DB operations and webhooks
# TODO: add in platform secrets (Vercel/GCP Secret Manager)
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME_SERVER_ONLY

# Payment provider secret (server-only)
PAYMENT_PROVIDER_SECRET=REPLACE_ME_SERVER_ONLY

# OAuth / Integrations
GOOGLE_OAUTH_CLIENT_ID=REPLACE_ME_SERVER_ONLY
GOOGLE_OAUTH_CLIENT_SECRET=REPLACE_ME_SERVER_ONLY
SLACK_WEBHOOK_URL=REPLACE_ME_SERVER_ONLY

# Sentry DSN (server-only or client, set per environment)
SENTRY_DSN=REPLACE_ME

# Vercel/GCP deployment tokens (server-side)
VERCEL_TOKEN=REPLACE_ME_SERVER_ONLY
GCP_SA_KEY=REPLACE_ME_SERVER_ONLY

# Optional: Stripe
STRIPE_SECRET=REPLACE_ME_SERVER_ONLY

# NOTE: Never add actual secrets to source or commit them; use platform-managed secrets
